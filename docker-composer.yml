version: "3"
services:
  nginx:
    image: nginx:alpine
    ports:
      - "8082:80"
    volumes:
      - /windows/www:/usr/share/nginx/html/:rw
      - ./nginx:/etc/nginx/conf.d/:ro
      #- ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - /var/log/nginx/:/var/log/nginx/:rw
    #privileged: true
    depends_on:
      - php-fpm
    networks:
      - lnmp
  php-fpm:
    image: swr.cn-north-1.myhuaweicloud.com/engine56/php:1.0.7
    expose:
      - "9000"
    volumes:
      - /windows/www:/var/www/html/:rw
      - ./php/php.ini:/usr/local/etc/php/php.ini:ro
      - ./php/php-fpm.conf:/usr/local/etc/php-fpm.d/www.conf:rw
      - /var/log/php-fpm/:/var/log/php-fpm/:rw
    #privileged: true
    networks:
      - lnmp
  mysql:
    image: mysql:latest
    ports:
      - "3307:3306"
    volumes:
      - ./mysql/my.cnf:/etc/mysql/my.cnf:ro
      - /home/www/mysql/data:/var/lib/mysql/:rw
      - /var/log/mysql/:/var/log/mysql/:rw
      - /home/www/mysql/mysql-files/:/var/lib/mysql-files:rw
    command: --default-authentication-plugin=mysql_native_password
    environment:
      MYSQL_ROOT_PASSWORD: "123456"
    #restart: always
    networks:
      - lnmp
  redis:
    image: redis:latest
    ports:
      - "6380:6379"
    networks:
      - lnmp
  adminer:
    image: adminer
    restart: always
    ports:
      - 8083:8080
    networks:
      - lnmp
networks:
  lnmp:
